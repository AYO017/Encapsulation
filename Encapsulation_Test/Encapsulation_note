				Encapsulation
👀 Encapsulation  in java is a mechanism of wrapping the data (variables).
👀In encapsulation the variables of a class will be hidden from other classes.
👀 it can be accessed only through the method of their current class.
👀 It is know as Data Hiding.
👀 writing data and code into a single unit.